# Gemini CLI Context Shim

This repository uses `AGENTS.md` as the primary project instruction file.

If you are using `Gemini CLI` in this workspace:

1. Read `AGENTS.md` first.
2. Treat `docs/` as raw sources and `wiki/` as the persistent wiki layer.
3. Read `README.md`, `wiki/README.md`, and `wiki/index.md` before deep work.
4. For the hands-on math agent prototype flow, use:
   - `docs/gemini-cli-math-agent-step-by-step.md`
5. For general local LLM wiki usage, use:
   - `docs/llm-wiki-usage-guide.md`

This file is a compatibility shim for Gemini CLI users.  
`AGENTS.md` remains the authoritative instruction file.
